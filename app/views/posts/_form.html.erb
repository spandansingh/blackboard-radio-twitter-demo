<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h4 class="text-danger"><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h4>

      <ul class="list-group">
        <% post.errors.full_messages.each do |message| %>
          <li class="list-group-item list-group-item-danger"><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= form.label 'Text' %>
    <%= form.text_area :text, class: 'form-control', placeholder: 'Write Text Here...' %>
  </div>
 
  <div class="form-group">
    <%= form.label 'Add Audio or Video' %>
    <%= form.file_field :link, class: 'form-control' %>
  </div>

  <%= form.submit "Save", class: 'btn btn-primary' %>
<% end %>